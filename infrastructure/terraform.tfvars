# Region and service configuration
region        = "europe-north2"  # Matching the region in your GitHub Actions workflow
service_name  = "feedback-analyzer"

# Resource allocation
memory        = "1Gi"
cpu           = "1"
min_instances = 0
max_instances = 10